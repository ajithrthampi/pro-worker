import Sidebar from '@/components/adminComponents/sidebar/Sidebar'
import { createFileRoute, Outlet } from '@tanstack/react-router'

export const Route = createFileRoute('/_adminlayout/admin/dashboard')({
  component: () => (
    <div className="flex">

    sadad
    dashboard1111
  </div>
  )
})